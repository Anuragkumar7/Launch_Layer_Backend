PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Render production: set CLIENT_URL to your live frontend URL
CLIENT_URL=https://launch-layer-business.vercel.app

# SMTP (Gmail, SendGrid, Mailgun, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=launchlayerbusiness@gmail.com
SMTP_PASS=mldcizahrofmaqik

# Where contact form emails are delivered
CONTACT_EMAIL=launchlayerbusiness@gmail.com

# Optional: send confirmation email to submitter
SEND_AUTO_REPLY=false
